const { verify } = require("crypto");
const { send } = require("process");

function loginIG(email, passwword) {
    geaFromEmail = verify.post(email, urlEmail);
    data = {};
    data.name = geaFromEmail.name
    data.age = geaFromEmail.age
    data.marital = geaFromEmail.marital

    database.post(data);
    dataFetched = database() {
        name >15,
        photo now
    }
 

    return dataFetched;



}